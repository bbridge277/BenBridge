<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Name — Portfolio</title>
  <meta name="description" content="Portfolio of a recent graduate — projects, experience, and contact." />
  <meta name="color-scheme" content="light dark" />
  <style>
    /* ============================
       THEME & TOKENS
       ============================ */
    :root{
      --bg: #0f1020;               /* deep space */
      --surface: #141627;          /* card */
      --text: #e8e9f0;             /* foreground */
      --muted: #a6a8bd;            /* subtle */
      --primary: #7c9cff;          /* bright indigo */
      --primary-600:#9bb2ff;
      --accent: #7ef2c4;           /* mint */
      --accent-600:#a9f6d6;
      --ring: rgba(255,255,255,.12);
      --shadow: 0 18px 50px rgba(0,0,0,.35);
      --radius: 18px;
      --grid: min(1100px, 92vw);
    }
    @media (prefers-color-scheme: light){
      :root{
        --bg: #f7f7fb;
        --surface: #ffffff;
        --text: #16181e;
        --muted: #61677c;
        --primary: #4158d0;
        --primary-600:#5f76ea;
        --accent: #10b981;
        --accent-600:#34d399;
        --ring: rgba(0,0,0,.08);
        --shadow: 0 16px 40px rgba(0,0,0,.08);
      }
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font-family: ui-sans-serif, system-ui, Segoe UI, Roboto, Helvetica, Arial, Apple Color Emoji, Segoe UI Emoji;
      color:var(--text);
      background: radial-gradient(1200px 700px at 10% -10%, color-mix(in oklab, var(--primary) 20%, transparent), transparent 70%),
                  radial-gradient(1000px 650px at 90% -10%, color-mix(in oklab, var(--accent) 18%, transparent), transparent 70%),
                  var(--bg);
      line-height:1.6; overflow-x:hidden;
    }
    a{color:inherit; text-decoration:none}
    img{max-width:100%; display:block}
    .container{width:var(--grid); margin-inline:auto}

    /* BUTTONS */
    .btn{display:inline-flex; align-items:center; gap:.6rem; padding:.85rem 1.05rem; border-radius:999px; border:1px solid var(--ring); background: var(--primary); color:#fff; font-weight:700; letter-spacing:.2px; box-shadow: var(--shadow); transition: transform .2s ease, box-shadow .2s ease, background .2s ease}
    .btn:hover{transform:translateY(-2px); box-shadow:0 24px 60px rgba(0,0,0,.18)}
    .btn--ghost{background:transparent; color:var(--text)}
    .chip{display:inline-flex; align-items:center; gap:.4rem; padding:.45rem .7rem; border-radius:999px; border:1px solid var(--ring); background: color-mix(in oklab, var(--surface) 60%, transparent)}

    /* HEADER */
    header{position:sticky; top:0; inset-inline:0; z-index:50; backdrop-filter:saturate(1.2) blur(8px); background: color-mix(in oklab, var(--bg) 75%, transparent); border-bottom:1px solid var(--ring)}
    .nav{display:flex; align-items:center; justify-content:space-between; gap:1rem; padding:.8rem 0}
    .brand{display:flex; align-items:center; gap:.8rem; font-weight:900}
    .logo{width:40px; height:40px; border-radius:12px; display:grid; place-items:center; background: conic-gradient(from 220deg, var(--primary), var(--accent)); box-shadow: inset 0 0 0 2px rgba(255,255,255,.15), var(--shadow)}
    .brand small{display:block; color:var(--muted); font-weight:600; letter-spacing:.3px}
    nav ul{display:flex; gap:1rem; align-items:center; list-style:none; margin:0; padding:0}
    nav a{padding:.55rem .8rem; border-radius:999px; border:1px solid transparent}
    nav a:hover{background: color-mix(in oklab, var(--primary) 16%, transparent); border-color:var(--ring)}
    .nav-actions{display:flex; gap:.6rem; align-items:center}

    /* MOBILE NAV */
    .burger{display:none; width:42px; height:42px; border-radius:12px; border:1px solid var(--ring); background: var(--surface)}
    .burger span{display:block; width:22px; height:2px; background:currentColor; margin:6px auto; border-radius:2px}
    @media (max-width: 900px){ nav ul{display:none} .burger{display:block} }

    /* HERO */
    .hero .container{display:grid; grid-template-columns:1.1fr .9fr; gap:2.2rem; align-items:center; padding:5.2rem 0 3rem}
    @media (max-width: 900px){ .hero .container{grid-template-columns:1fr; padding:3.6rem 0 2rem} }
    .kicker{text-transform:uppercase; letter-spacing:2px; font-weight:800; color:var(--accent); font-size:.9rem}
    .headline{font-size: clamp(2.2rem, 3.4vw + 1rem, 4rem); line-height:1.05; margin:0 0 1rem; letter-spacing:-.6px}
    .subhead{color:var(--muted); font-size: clamp(1rem, .25vw + 1rem, 1.2rem); margin:.8rem 0 1.4rem}

    /* HERO CARD */
    .hero-card{position:relative; background: var(--surface); border:1px solid var(--ring); border-radius: var(--radius); padding:1rem; overflow:hidden; box-shadow: var(--shadow)}
    .profile{display:grid; grid-template-columns: 1fr; gap:.8rem; align-content:start}
    .avatar{aspect-ratio: 4/3; border-radius: 14px; background: linear-gradient(135deg, color-mix(in oklab, var(--primary) 30%, var(--surface)), color-mix(in oklab, var(--accent) 35%, var(--surface))); border:1px solid var(--ring); position:relative; overflow:hidden}
    .badge{position:absolute; bottom:.6rem; right:.6rem; background: var(--surface); border:1px solid var(--ring); padding:.25rem .5rem; border-radius:999px; font-size:.75rem}

    /* SECTIONS */
    section{padding:3.5rem 0; position:relative}
    .section-title{font-size:clamp(1.6rem, 1.2vw + 1rem, 2.1rem); margin:0 0 1rem}
    .eyebrow{color:var(--accent); text-transform:uppercase; letter-spacing:2px; font-weight:800; font-size:.85rem}
    .muted{color:var(--muted)}

    /* GRID */
    .grid{display:grid; gap:1.1rem}
    .grid.auto-fit{grid-template-columns: repeat(auto-fit, minmax(240px, 1fr))}
    .card{background: var(--surface); border:1px solid var(--ring); border-radius: var(--radius); padding:1.1rem; box-shadow: var(--shadow)}

    /* PROJECTS */
    .filters{display:flex; gap:.4rem; flex-wrap:wrap; margin-bottom:.8rem}
    .pill{padding:.45rem .7rem; border-radius:999px; border:1px solid var(--ring); cursor:pointer; user-select:none}
    .pill.active{background: color-mix(in oklab, var(--primary) 22%, transparent)}
    .project{display:grid; gap:.6rem; align-content:start; transition: transform .2s ease}
    .project:hover{transform: translateY(-4px)}
    .proj-thumb{aspect-ratio: 16/10; border-radius:14px; overflow:hidden; border:1px solid var(--ring); position:relative}
    .proj-thumb::after{content:""; position:absolute; inset:0; background: linear-gradient(180deg, transparent, rgba(0,0,0,.35)); opacity:.4}

    /* TIMELINE */
    .timeline{position:relative; margin:1rem 0}
    .timeline::before{content:""; position:absolute; left:12px; top:0; bottom:0; width:3px; background: linear-gradient(var(--accent), transparent)}
    .tl-item{display:grid; grid-template-columns:28px 1fr; gap:.9rem; padding:.6rem 0}
    .tl-dot{width:24px; height:24px; border-radius:50%; background: var(--accent); box-shadow:0 0 0 4px color-mix(in oklab, var(--accent) 20%, transparent)}

    /* SKILLS */
    .skill{display:grid; gap:.25rem}
    .meter{height:10px; border-radius:999px; background: color-mix(in oklab, var(--surface) 65%, transparent); border:1px solid var(--ring); overflow:hidden}
    .bar{height:100%; width:0%; background: linear-gradient(90deg, var(--primary), var(--accent)); transition: width 1s ease}

    /* CONTACT */
    form{display:grid; gap:.8rem; max-width:700px}
    input, textarea{background: var(--surface); color:var(--text); border:1px solid var(--ring); border-radius:12px; padding:.85rem .9rem; font:inherit}
    textarea{min-height:120px; resize:vertical}

    /* FOOTER */
    footer{padding:3rem 0 4rem; border-top:1px solid var(--ring); margin-top:2rem; background: color-mix(in oklab, var(--bg) 85%, transparent)}

    /* REVEAL */
    .reveal{opacity:0; transform: translateY(16px); transition: opacity .6s ease, transform .6s ease}
    .reveal.in{opacity:1; transform: translateY(0)}

    /* MODAL */
    dialog.lightbox{border:none; padding:0; border-radius:18px; max-width:min(92vw, 1000px); background: var(--surface)}
    dialog::backdrop{background: rgba(0,0,0,.45)}
    .lightbox .close{position:absolute; top:.6rem; right:.6rem; border:1px solid var(--ring); background: var(--surface); border-radius:10px; padding:.4rem .6rem}

    /* MODE TOGGLE */
    .toggle{border:1px solid var(--ring); background: var(--surface); border-radius:999px; padding:.4rem .6rem; display:flex; gap:.45rem; align-items:center}

    /* UTIL */
    .center{text-align:center}
    .stack{display:grid; gap:.35rem}
  </style>
</head>
<body>
  <canvas id="bgOrbs" aria-hidden="true"></canvas>

  <header>
    <div class="container nav" role="navigation" aria-label="Main">
      <a class="brand" href="#top" aria-label="Home">
        <span class="logo" aria-hidden="true">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <circle cx="12" cy="12" r="10" fill="url(#g)"/>
            <defs>
              <radialGradient id="g" cx="0" cy="0" r="1" gradientUnits="userSpaceOnUse" gradientTransform="translate(12 12) rotate(90) scale(10)">
                <stop stop-color="#7c9cff"/>
                <stop offset="1" stop-color="#10b981"/>
              </radialGradient>
            </defs>
          </svg>
        </span>
        <span>Your Name<small>Recent Graduate • Portfolio</small></span>
      </a>

      <nav>
        <ul id="navLinks">
          <li><a href="#about">About</a></li>
          <li><a href="#projects">Projects</a></li>
          <li><a href="#experience">Experience</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>

      <div class="nav-actions">
        <button class="toggle" id="modeToggle" aria-label="Toggle color mode" title="Toggle color mode">
          <span aria-hidden="true">🌞</span><span aria-hidden="true">/</span><span aria-hidden="true">🌙</span>
        </button>
        <a class="btn btn--ghost" href="#resume" id="resumeBtn">Résumé</a>
        <button class="burger" id="burger" aria-label="Open menu" aria-controls="navLinks" aria-expanded="false">
          <span></span><span></span><span></span>
        </button>
      </div>
    </div>
  </header>

  <main id="top">
    <!-- HERO -->
    <section class="hero">
      <div class="container">
        <div>
          <div class="kicker">Hello 👋</div>
          <h1 class="headline">I design & build useful things — from data-driven experiments to human‑friendly web apps.</h1>
          <p class="subhead">I’m a recent graduate looking for a role where I can learn fast, ship great work, and make a positive impact. Here’s a selection of projects and experience that show how I think and build.</p>
          <div class="cta" style="display:flex; gap:.8rem; flex-wrap:wrap">
            <a class="btn" href="#projects">See my projects</a>
            <a class="btn btn--ghost" href="#contact">Get in touch</a>
          </div>
        </div>
        <div class="hero-card reveal">
          <div class="profile">
            <div class="avatar" role="img" aria-label="Your portrait placeholder">
              <div class="badge">Open to work</div>
            </div>
            <div class="grid auto-fit">
              <span class="chip">Based in • Your City</span>
              <span class="chip">Email • you@domain.com</span>
              <span class="chip">LinkedIn • /in/your-handle</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ABOUT -->
    <section id="about">
      <div class="container">
        <p class="eyebrow">About</p>
        <h2 class="section-title">A quick intro</h2>
        <p class="muted">I love hard problems with real‑world impact. My background combines hands‑on experimentation, data analysis, and front‑end engineering. I value clear communication, thoughtful design, and teamwork.</p>

        <div class="grid auto-fit" style="margin-top:1.1rem">
          <div class="card reveal">
            <h3>Strengths</h3>
            <div class="grid">
              <div class="skill"><strong>JavaScript / TypeScript</strong><div class="meter"><div class="bar" data-pct="85"></div></div></div>
              <div class="skill"><strong>HTML / CSS</strong><div class="meter"><div class="bar" data-pct="90"></div></div></div>
              <div class="skill"><strong>Python / Data</strong><div class="meter"><div class="bar" data-pct="75"></div></div></div>
              <div class="skill"><strong>UX & Accessibility</strong><div class="meter"><div class="bar" data-pct="70"></div></div></div>
            </div>
          </div>
          <div class="card reveal">
            <h3>Highlights</h3>
            <ul class="muted" style="margin:0; padding-left:1.1rem">
              <li>Shipped 6+ projects with real users</li>
              <li>Led a small team capstone from idea → demo</li>
              <li>Internship experience across research & product</li>
            </ul>
            <p style="margin:.8rem 0 0"><a class="btn" href="#projects">Browse work</a></p>
          </div>
        </div>
      </div>
    </section>

    <!-- PROJECTS -->
    <section id="projects">
      <div class="container">
        <p class="eyebrow">Projects</p>
        <h2 class="section-title">Selected work</h2>
        <div class="filters" role="tablist" aria-label="Project filters">
          <span class="pill active" data-filter="all" role="tab" aria-selected="true">All</span>
          <span class="pill" data-filter="web" role="tab">Web</span>
          <span class="pill" data-filter="data" role="tab">Data</span>
          <span class="pill" data-filter="research" role="tab">Research</span>
        </div>
        <div class="grid auto-fit" id="projectGrid">
          <!-- PROJECT CARD TEMPLATE-->
          <article class="card reveal project" data-tags="web">
            <div class="proj-thumb">
              <img alt="Project thumbnail" src="data:image/svg+xml;utf8,\
                <svg xmlns='http://www.w3.org/2000/svg' width='1200' height='750'>\
                  <defs><linearGradient id='g1' x1='0' x2='1' y1='0' y2='1'>\
                    <stop offset='0' stop-color='%237c9cff'/>\
                    <stop offset='1' stop-color='%2310b981'/>\
                  </linearGradient></defs>\
                  <rect width='100%' height='100%' fill='url(%23g1)'/>\
                  <text x='50%' y='50%' dominant-baseline='middle' text-anchor='middle' font-family='system-ui' font-size='48' fill='white'>Web App</text>\
                </svg>" />
            </div>
            <h3>TaskFlow — Minimal project tracker</h3>
            <p class="muted">A lightweight Kanban app with offline‑ready caching and keyboard shortcuts.</p>
            <div class="stack" style="grid-auto-flow:column; grid-auto-columns:max-content; display:inline-grid">
              <span class="chip">TypeScript</span><span class="chip">PWA</span><span class="chip">IndexedDB</span>
            </div>
            <p style="margin:.6rem 0 0; display:flex; gap:.5rem; flex-wrap:wrap">
              <a class="btn btn--ghost" href="#" data-open="TaskFlow">Details</a>
              <a class="btn" href="#">Live</a>
            </p>
          </article>

          <article class="card reveal project" data-tags="data research">
            <div class="proj-thumb">
              <img alt="Project thumbnail" src="data:image/svg+xml;utf8,\
                <svg xmlns='http://www.w3.org/2000/svg' width='1200' height='750'>\
                  <rect width='100%' height='100%' fill='%23141527'/>\
                  <g fill='%237c9cff'>\
                    <rect x='120' y='500' width='120' height='200'/>\
                    <rect x='280' y='420' width='120' height='280'/>\
                    <rect x='440' y='340' width='120' height='360'/>\
                    <rect x='600' y='260' width='120' height='440'/>\
                  </g>\
                  <text x='50%' y='70' text-anchor='middle' font-family='system-ui' font-size='40' fill='%23a6a8bd'>Data Viz</text>\
                </svg>" />
            </div>
            <h3>CitySense — Environmental dashboard</h3>
            <p class="muted">Built a dashboard that visualizes air & noise sensors; added anomaly detection to flag spikes.</p>
            <div class="stack" style="display:inline-grid; grid-auto-flow:column; grid-auto-columns:max-content">
              <span class="chip">Python</span><span class="chip">D3.js</span><span class="chip">Pandas</span>
            </div>
            <p style="margin:.6rem 0 0; display:flex; gap:.5rem; flex-wrap:wrap">
              <a class="btn btn--ghost" href="#" data-open="CitySense">Details</a>
              <a class="btn" href="#">Repo</a>
            </p>
          </article>

          <article class="card reveal project" data-tags="research">
            <div class="proj-thumb">
              <img alt="Project thumbnail" src="data:image/svg+xml;utf8,\
                <svg xmlns='http://www.w3.org/2000/svg' width='1200' height='750'>\
                  <defs><linearGradient id='g2' x1='0' x2='0' y1='0' y2='1'>\
                    <stop offset='0' stop-color='%2310b981'/>\
                    <stop offset='1' stop-color='%23141627'/>\
                  </linearGradient></defs>\
                  <rect width='100%' height='100%' fill='url(%23g2)'/>\
                  <text x='50%' y='50%' dominant-baseline='middle' text-anchor='middle' font-family='system-ui' font-size='44' fill='white'>Research</text>\
                </svg>" />
            </div>
            <h3>Capstone — Smart irrigation prototype</h3>
            <p class="muted">Designed a moisture‑aware controller; reduced water usage by ~23% in small plots.</p>
            <div class="stack" style="display:inline-grid; grid-auto-flow:column; grid-auto-columns:max-content">
              <span class="chip">IoT</span><span class="chip">Node</span><span class="chip">Sensors</span>
            </div>
            <p style="margin:.6rem 0 0; display:flex; gap:.5rem; flex-wrap:wrap">
              <a class="btn btn--ghost" href="#" data-open="Irrigation">Details</a>
              <a class="btn" href="#">Paper</a>
            </p>
          </article>
        </div>

        <dialog class="lightbox" id="projModal">
          <button class="close" aria-label="Close">✕</button>
          <div class="container" style="padding:1rem">
            <h3 id="modalTitle">Project</h3>
            <p class="muted" id="modalBody">Details go here.</p>
            <p><a id="modalLink" class="btn" href="#" target="_blank" rel="noopener">View</a></p>
          </div>
        </dialog>
      </div>
    </section>

    <!-- EXPERIENCE -->
    <section id="experience">
      <div class="container">
        <p class="eyebrow">Experience</p>
        <h2 class="section-title">Roles & education</h2>
        <div class="timeline">
          <div class="tl-item reveal">
            <div class="tl-dot" aria-hidden="true"></div>
            <div>
              <strong>Front-End Intern — Company XYZ</strong>
              <div class="muted">Summer 2024 • Built components, improved performance by 30%</div>
            </div>
          </div>
          <div class="tl-item reveal">
            <div class="tl-dot" aria-hidden="true"></div>
            <div>
              <strong>Undergraduate Researcher — Lab ABC</strong>
              <div class="muted">2023–2024 • Data pipelines, visualization, and write‑ups</div>
            </div>
          </div>
          <div class="tl-item reveal">
            <div class="tl-dot" aria-hidden="true"></div>
            <div>
              <strong>B.S. — Your Major, Your University</strong>
              <div class="muted">Class of 2025 • GPA 3.8 • Relevant coursework: Algorithms, HCI, Databases</div>
            </div>
          </div>
        </div>
        <div style="display:flex; gap:.6rem; flex-wrap:wrap">
          <a id="resume" class="btn" href="#" download>Download résumé (PDF)</a>
          <a class="btn btn--ghost" href="#contact">Let’s talk</a>
        </div>
      </div>
    </section>

    <!-- CONTACT -->
    <section id="contact">
      <div class="container">
        <p class="eyebrow">Contact</p>
        <h2 class="section-title">Say hello</h2>
        <form class="reveal" id="contactForm">
          <div class="grid auto-fit">
            <label class="stack">Name
              <input name="name" required placeholder="Your name" />
            </label>
            <label class="stack">Email
              <input type="email" name="email" required placeholder="you@domain.com" />
            </label>
          </div>
          <label class="stack">Message
            <textarea name="message" required placeholder="What would you like to build?"></textarea>
          </label>
          <div style="display:flex; gap:.6rem; align-items:center; flex-wrap:wrap">
            <button class="btn" type="submit">Send</button>
            <span class="muted" id="formStatus" role="status" aria-live="polite"></span>
          </div>
        </form>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <div class="grid auto-fit">
        <div>
          <div class="brand" style="margin-bottom:.6rem">
            <span class="logo" aria-hidden="true"></span>
            <strong>Your Name</strong>
          </div>
          <p class="muted">© <span id="year"></span> • Built with HTML/CSS/JS — no frameworks.</p>
        </div>
        <div>
          <strong>Links</strong>
          <div class="stack">
            <a href="#projects">Projects</a>
            <a href="#experience">Experience</a>
            <a href="#contact">Contact</a>
          </div>
        </div>
        <div>
          <strong>Social</strong>
          <div class="stack">
            <a href="#">GitHub</a>
            <a href="#">LinkedIn</a>
            <a href="#">Email</a>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <script>
    // ============================
    // INTERACTIVITY & ANIMATIONS
    // ============================

    // Mobile menu
    const burger = document.getElementById('burger');
    const navLinks = document.getElementById('navLinks');
    burger?.addEventListener('click', () => {
      const open = burger.getAttribute('aria-expanded') === 'true';
      burger.setAttribute('aria-expanded', String(!open));
      if (!open) {
        navLinks.style.display = 'grid';
        navLinks.style.gridTemplateColumns = '1fr';
        navLinks.style.gap = '.4rem';
        navLinks.style.padding = '.6rem';
        navLinks.style.background = getComputedStyle(document.documentElement).getPropertyValue('--surface');
        navLinks.style.border = '1px solid var(--ring)';
        navLinks.style.borderRadius = '16px';
        navLinks.style.position = 'absolute';
        navLinks.style.top = '60px';
        navLinks.style.right = '12px';
        navLinks.style.boxShadow = 'var(--shadow)';
      } else {
        navLinks.removeAttribute('style');
      }
    });

    // Color mode toggle
    const modeToggle = document.getElementById('modeToggle');
    const applyTheme = (theme) => {
      document.documentElement.dataset.theme = theme;
      document.documentElement.style.colorScheme = theme === 'dark' ? 'dark' : 'light';
    }
    try { const saved = localStorage.getItem('theme'); if (saved) applyTheme(saved); } catch {}
    modeToggle?.addEventListener('click', () => {
      const current = document.documentElement.dataset.theme || (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
      const next = current === 'dark' ? 'light' : 'dark';
      applyTheme(next); try { localStorage.setItem('theme', next); } catch {}
    });

    // Reveal on scroll
    const revealEls = document.querySelectorAll('.reveal');
    const io = new IntersectionObserver((entries) => {
      for (const e of entries) { if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } }
    }, { threshold: .18 });
    revealEls.forEach(el => io.observe(el));

    // Animate skill meters when visible
    document.querySelectorAll('.bar').forEach(bar => {
      const pct = +bar.dataset.pct || 0;
      const obs = new IntersectionObserver((entries, o) => {
        if (entries[0].isIntersecting) { bar.style.width = pct + '%'; o.disconnect(); }
      }, { threshold: .5 });
      obs.observe(bar);
    });

    // Project filter
    const pills = document.querySelectorAll('.pill');
    const grid = document.getElementById('projectGrid');
    pills.forEach(p => p.addEventListener('click', () => {
      pills.forEach(x => x.classList.remove('active')); p.classList.add('active');
      const tag = p.dataset.filter;
      grid.querySelectorAll('.project').forEach(card => {
        const show = tag === 'all' || card.dataset.tags.includes(tag);
        card.style.display = show ? '' : 'none';
      });
    }));

    // Project modal
    const modal = document.getElementById('projModal');
    const mTitle = document.getElementById('modalTitle');
    const mBody = document.getElementById('modalBody');
    const mLink = document.getElementById('modalLink');
    const projectCopy = {
      TaskFlow: {
        title: 'TaskFlow — Minimal project tracker',
        body: 'Built a snappy PWA with offline support and keyboard shortcuts. Users can create boards, drag tasks, and sync when online again. I focused on accessibility and performance (100 Lighthouse PWA score).',
        link: '#'
      },
      CitySense: {
        title: 'CitySense — Environmental dashboard',
        body: 'Multi-sensor ingestion, rolling z-score anomaly detection, and responsive D3 visualizations for city planners. Includes CSV import and export.',
        link: '#'
      },
      Irrigation: {
        title: 'Smart irrigation prototype',
        body: 'Arduino-based controller that uses soil moisture thresholds and simple evapotranspiration estimates to schedule watering. Logged data to a small web app for analysis; reduced water usage by ~23% in test plots.',
        link: '#'
      }
    };
    document.querySelectorAll('[data-open]').forEach(btn => btn.addEventListener('click', (e) => {
      e.preventDefault();
      const key = btn.getAttribute('data-open');
      const { title, body, link } = projectCopy[key] || { title: 'Project', body: 'Details coming soon', link: '#' };
      mTitle.textContent = title; mBody.textContent = body; mLink.href = link; modal.showModal();
    }));
    modal?.querySelector('.close')?.addEventListener('click', () => modal.close());
    modal?.addEventListener('click', (e) => { if (e.target === modal) modal.close(); });

    // Contact form (demo only)
    const form = document.getElementById('contactForm');
    const status = document.getElementById('formStatus');
    form?.addEventListener('submit', (e) => {
      e.preventDefault();
      const data = Object.fromEntries(new FormData(form).entries());
      status.textContent = 'Sending…';
      setTimeout(() => { status.textContent = 'Thanks! I\'ll reply soon.'; form.reset(); }, 700);
      console.log('Contact form submission', data);
    });

    // Footer year
    document.getElementById('year').textContent = new Date().getFullYear();

    // Background animated orbs (canvas)
    const canvas = document.getElementById('bgOrbs');
    const ctx = canvas.getContext('2d');
    const resize = () => { canvas.width = innerWidth; canvas.height = Math.max(900, innerHeight); };
    addEventListener('resize', resize); resize();
    const orbs = Array.from({length: 18}, () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height * 0.6,
      r: 40 + Math.random()*120,
      s: 0.2 + Math.random()*0.8,
      hue: Math.random() < .5 ? 'rgba(124,156,255,0.10)' : 'rgba(16,185,129,0.10)'
    }));
    let last = 0;
    const mq = matchMedia('(prefers-reduced-motion: reduce)');
    function loop(t){
      const dt = Math.min((t - last)/16, 2); last = t;
      ctx.clearRect(0,0,canvas.width,canvas.height);
      for (const o of orbs){
        o.y += o.s * dt; o.x += Math.sin((o.y + t*.02) * 0.005) * o.s * 0.8;
        if (o.y - o.r > canvas.height) { o.y = -o.r; o.x = Math.random()*canvas.width; }
        const g = ctx.createRadialGradient(o.x, o.y, 0, o.x, o.y, o.r);
        g.addColorStop(0, o.hue);
        g.addColorStop(1, 'rgba(0,0,0,0)');
        ctx.fillStyle = g; ctx.beginPath(); ctx.arc(o.x, o.y, o.r, 0, Math.PI*2); ctx.fill();
      }
      requestAnimationFrame(loop);
    }
    if (!mq.matches) requestAnimationFrame(loop);

    // Smooth scroll for internal links
    document.querySelectorAll('a[href^="#"]').forEach(a => a.addEventListener('click', e => {
      const id = a.getAttribute('href');
      if (id.length > 1) { e.preventDefault(); document.querySelector(id)?.scrollIntoView({behavior:'smooth'}); }
    }));

    // Résumé button (replace link)
    document.getElementById('resumeBtn')?.addEventListener('click', (e) => {
      // If you have a file, set href on #resume above. This keeps the top button in sync.
      const res = document.getElementById('resume');
      const href = res?.getAttribute('href') || '#';
      if (href === '#') return; // no file yet
      e.preventDefault(); window.open(href, '_blank');
    });
  </script>
</body>
</html>
